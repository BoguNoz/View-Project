<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="View.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:View"
     xmlns:helpers="clr-namespace:View.Helpers"
    Shell.FlyoutBehavior="Locked"
    FlyoutWidth="65"
    FlyoutBackgroundColor="#030009"
    Title="View">

    <Shell.Resources>
        <Style Class="FlyoutItemLabelStyle" TargetType="Label">
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="TextColor" Value="#99FFFFFF" />
        </Style>
    </Shell.Resources>


    <FlyoutItem Title="Home" >
        <FlyoutItem.FlyoutIcon>
            <FontImageSource
                FontFamily="FontSolid"
                Glyph="{x:Static helpers:FontSolid.House}"
                Color="#3700B3"/>
        </FlyoutItem.FlyoutIcon>
        <ShellContent ContentTemplate="{DataTemplate local:MainPage}"/>
    </FlyoutItem>

    
    <FlyoutItem Title="Model">
        <FlyoutItem.FlyoutIcon>
            <FontImageSource
                FontFamily="FontSolid"
                Glyph="{x:Static helpers:FontSolid.Database}"
                Color="#E6FFFFFF"/>
        </FlyoutItem.FlyoutIcon>
        <ShellContent ContentTemplate="{DataTemplate local:SchemaPage}"/>
    </FlyoutItem>

    
    <FlyoutItem Title="Data">
        <FlyoutItem.FlyoutIcon>
            <FontImageSource
                FontFamily="FontSolid"
                Glyph="{x:Static helpers:FontSolid.Table}"
                Color="#E6FFFFFF"/>
        </FlyoutItem.FlyoutIcon>
        <ShellContent ContentTemplate="{DataTemplate local:TablePage}"/>
    </FlyoutItem>




    <Shell.FlyoutFooter>
        <Grid Padding="25">
            <Grid.GestureRecognizers>
                <TapGestureRecognizer Tapped="TapGestureRecognizer_Tapped" />
            </Grid.GestureRecognizers>
            <Label x:Name="Expand" FontFamily="FontSolid" FontSize="15" TextColor="#E6FFFFFF" Text="{x:Static helpers:FontSolid.ArrowRight}"/>
            <Label x:Name="Minimize" FontFamily="FontSolid" FontSize="15"  TextColor="#E6FFFFFF" IsVisible="false" Text="{x:Static helpers:FontSolid.ArrowLeft}"/>
        </Grid>
    </Shell.FlyoutFooter>
    
</Shell>
